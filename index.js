console.log('hello!');

function add(a, b) {
  return a + b;
}

function log(message) {
  console.log(message);
}

// 1. Create a new branch
// 2. Add and commit a change to that branch
// 3. push the change to your remote branch (will be a new branch)
// 4. make a PR for your new change into master (or main, whatever)
// 5  Sync up your local with your remote, then delete unnecessary branches
